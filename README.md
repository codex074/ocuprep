# 📋 UTTH ED-Extemp — เอกสารสรุประบบ

> **ระบบบันทึกการผลิตยาหยอดตาเฉพาะราย (Extemporaneous Eye Drop Preparation)**
> กลุ่มงานเภสัชกรรม โรงพยาบาลอุตรดิตถ์

---

## 📖 สารบัญ

1. [ภาพรวมของระบบ](#-ภาพรวมของระบบ)
2. [ฟีเจอร์หลัก](#-ฟีเจอร์หลัก)
3. [Technology Stack](#-technology-stack)
4. [System Requirements](#-system-requirements)
5. [คู่มือการติดตั้ง](#-คู่มือการติดตั้ง)
6. [โครงสร้างโปรเจกต์](#-โครงสร้างโปรเจกต์)
7. [ฐานข้อมูล](#-ฐานข้อมูล)
8. [การ Deploy](#-การ-deploy)

---

## 🏥 ภาพรวมของระบบ

**UTTH ED-Extemp** (Uttaradit Hospital Emergency Department — Extemporaneous Preparations) เป็นเว็บแอปพลิเคชันสำหรับ **บันทึก ติดตาม และจัดการการผลิตยาหยอดตาเฉพาะราย** ในกลุ่มงานเภสัชกรรม โรงพยาบาลอุตรดิตถ์

### การทำงานหลักของระบบ

```
ผู้ใช้ (เภสัชกร) → Login → เลือกห้องทำงาน (Station)
    → Dashboard (ภาพรวม)
    → บันทึกการเตรียมยา (Prepare) → พิมพ์ฉลาก/Batch Sheet
    → ดูประวัติการเตรียม (History) → แก้ไข/พิมพ์ซ้ำ
    → จัดการสูตรยา (Formulas)
    → จัดการผู้ใช้ (Users) [Admin only]
```

### ฟีเจอร์ความปลอดภัย

- ระบบ Login ด้วย **รหัสเภสัชกร (PHA ID)** และรหัสผ่าน
- บังคับเปลี่ยนรหัสผ่านเมื่อเข้าใช้ครั้งแรก (`must_change_password`)
- แบ่งสิทธิ์ผู้ใช้เป็น **Admin** และ **User**
- Admin สามารถแก้ไขข้อมูลทั้งหมด / User แก้ไขได้เฉพาะข้อมูลตนเอง

---

## ⭐ ฟีเจอร์หลัก

| ฟีเจอร์                             | รายละเอียด                                                           |
| ----------------------------------- | -------------------------------------------------------------------- |
| **🔐 Login**                        | เข้าสู่ระบบด้วยรหัสเภสัชกร + รหัสผ่าน, บังคับเปลี่ยนรหัสผ่านครั้งแรก |
| **🏠 Dashboard**                    | แสดงสถิติรวม, กิจกรรมล่าสุด, Quick Actions                           |
| **💊 Prepare (บันทึกการเตรียมยา)**  | เลือกสูตรยา, ใส่ข้อมูลผู้ป่วยหรือ Stock, กำหนด Lot No. / วันหมดอายุ  |
| **🏷️ Label & Batch Sheet Printing** | พิมพ์ฉลากยาติดขวด และ Batch Sheet สำหรับบันทึกกระบวนการ              |
| **📜 History**                      | ดูประวัติการเตรียมยาทั้งหมด, กรอง, ค้นหา, แก้ไข, พิมพ์ซ้ำ            |
| **🧪 Formula Management**           | เพิ่ม/แก้ไข/ลบสูตรยา พร้อมส่วนประกอบและวิธีทำ                        |
| **👥 User Management**              | (Admin) เพิ่ม/แก้ไขผู้ใช้, กำหนดสิทธิ์, รีเซ็ตรหัสผ่าน               |
| **👤 Profile**                      | ดูและแก้ไขโปรไฟล์ตนเอง                                               |
| **📱 PWA**                          | ติดตั้งเป็นแอปบนมือถือ/แท็บเล็ตได้, ใช้งาน Offline บางส่วน           |
| **📊 Export**                       | ส่งออกข้อมูลเป็น Excel (xlsx)                                        |

---

## 🛠 Technology Stack

### Frontend

| เทคโนโลยี            | เวอร์ชัน | หน้าที่                           |
| -------------------- | -------- | --------------------------------- |
| **React**            | 19.2.0   | UI Framework                      |
| **TypeScript**       | 5.9.3    | Type-safe JavaScript              |
| **Vite**             | 7.3.1    | Build tool & Dev server           |
| **React Router DOM** | 7.13.0   | Client-side routing (Hash Router) |
| **SweetAlert2**      | 11.26.18 | สำหรับ Dialog / Confirm pop-up    |
| **xlsx (SheetJS)**   | 0.18.5   | Export ข้อมูลเป็น Excel           |
| **vite-plugin-pwa**  | 1.2.0    | Progressive Web App support       |

### Backend / Database

| เทคโนโลยี                    | หน้าที่                                      |
| ---------------------------- | -------------------------------------------- |
| **Supabase**                 | Backend-as-a-Service (PostgreSQL + REST API) |
| **PostgreSQL**               | ฐานข้อมูลหลัก (hosted on Supabase)           |
| **Row Level Security (RLS)** | ควบคุมสิทธิ์การเข้าถึงข้อมูลระดับแถว         |

### Hosting / Deployment

| เทคโนโลยี        | หน้าที่                                 |
| ---------------- | --------------------------------------- |
| **GitHub Pages** | Static hosting (deploy ด้วย `gh-pages`) |

### UI Design

- Modern **Glassmorphism** design
- Responsive (Desktop / Tablet / Mobile)
- Dark gradient backgrounds
- Micro-animations & hover effects

---

## 💻 System Requirements

### สำหรับผู้พัฒนา (Development)

| รายการ          | ความต้องการขั้นต่ำ                                   |
| --------------- | ---------------------------------------------------- |
| **OS**          | macOS, Windows 10+, หรือ Linux                       |
| **Node.js**     | เวอร์ชัน **18.0** ขึ้นไป (แนะนำ 20 LTS หรือใหม่กว่า) |
| **npm**         | เวอร์ชัน **9.0** ขึ้นไป (มาพร้อมกับ Node.js)         |
| **Git**         | เวอร์ชัน 2.x ขึ้นไป                                  |
| **IDE/Editor**  | VS Code (แนะนำ) หรือ editor อื่น ๆ                   |
| **RAM**         | อย่างน้อย 4 GB                                       |
| **Disk Space**  | อย่างน้อย 500 MB (สำหรับ node_modules + build)       |
| **เบราว์เซอร์** | Chrome, Firefox, Safari, หรือ Edge เวอร์ชันล่าสุด    |

### สำหรับผู้ใช้งาน (End User)

| รายการ           | ความต้องการ                                               |
| ---------------- | --------------------------------------------------------- |
| **เบราว์เซอร์**  | Chrome 90+, Firefox 90+, Safari 14+, หรือ Edge 90+        |
| **อุปกรณ์**      | คอมพิวเตอร์, แท็บเล็ต, หรือมือถือ                         |
| **อินเทอร์เน็ต** | จำเป็น (สำหรับเชื่อมต่อ Supabase)                         |
| **เครื่องพิมพ์** | สำหรับการพิมพ์ฉลากยาและ Batch Sheet (แนะนำ Laser Printer) |

### บริการภายนอก (External Services)

| รายการ               | รายละเอียด                                   |
| -------------------- | -------------------------------------------- |
| **Supabase Account** | สำหรับ PostgreSQL database + REST API        |
| **GitHub Account**   | สำหรับ version control และ deploy (gh-pages) |

---

## 📥 คู่มือการติดตั้ง

### ขั้นตอนที่ 1: ติดตั้ง Prerequisites

#### 1.1 ติดตั้ง Node.js

**macOS** (ด้วย Homebrew):

```bash
brew install node
```

**Windows**:

- ดาวน์โหลดจาก [https://nodejs.org](https://nodejs.org) แล้วติดตั้งตามขั้นตอน

**ตรวจสอบการติดตั้ง**:

```bash
node -v    # ควรได้ v18.x ขึ้นไป
npm -v     # ควรได้ v9.x ขึ้นไป
```

#### 1.2 ติดตั้ง Git

```bash
# macOS
brew install git

# Windows — ดาวน์โหลดจาก https://git-scm.com

# ตรวจสอบ
git --version
```

---

### ขั้นตอนที่ 2: Clone โปรเจกต์

```bash
git clone https://github.com/codex074/ocuprep.git
cd ocuprep
```

---

### ขั้นตอนที่ 3: ติดตั้ง Dependencies

```bash
npm install
```

> คำสั่งนี้จะติดตั้ง packages ทั้งหมดที่ระบุใน `package.json` ลงในโฟลเดอร์ `node_modules/`

---

### ขั้นตอนที่ 4: ตั้งค่า Environment Variables

สร้างไฟล์ `.env` ที่ root ของโปรเจกต์:

```bash
touch .env
```

เพิ่มเนื้อหาด้านล่าง (แทนที่ด้วยค่าจริงของคุณ):

```env
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-supabase-anon-key
```

> ⚠️ **หมายเหตุ**: อย่า commit ไฟล์ `.env` ขึ้น git repository (ควรเพิ่มใน `.gitignore`)

---

### ขั้นตอนที่ 5: ตั้งค่า Supabase Database

#### 5.1 สร้าง Supabase Project

1. ไปที่ [https://supabase.com](https://supabase.com) และสร้างบัญชี/โปรเจกต์
2. คัดลอก **Project URL** และ **Anon Key** จากหน้า Settings > API
3. ใส่ค่าในไฟล์ `.env` ตามขั้นตอนที่ 4

#### 5.2 สร้างตาราง

เปิด **SQL Editor** ใน Supabase Dashboard แล้วรันไฟล์ SQL ตามลำดับ:

**1) Schema หลัก** — `supabase_schema.sql`:

```sql
-- สร้างตาราง users, formulas, preps
-- พร้อม Seed Data (ข้อมูลตัวอย่าง)
-- เปิดใช้งาน Row Level Security (RLS)
```

**2) Migration เพิ่มเติม** — `supabase_migration_add_password_change.sql`:

```sql
-- เพิ่มคอลัมน์ must_change_password ในตาราง users
ALTER TABLE public.users ADD COLUMN IF NOT EXISTS must_change_password BOOLEAN DEFAULT false;
```

---

### ขั้นตอนที่ 6: รันแอปพลิเคชัน (Development Mode)

```bash
npm run dev
```

แอปจะเปิดที่ `http://localhost:5173` (หรือ port อื่นที่ว่าง)

#### บัญชีทดสอบเริ่มต้น (จาก Seed Data):

| ชื่อ             | PHA ID | รหัสผ่าน | Role  |
| ---------------- | ------ | -------- | ----- |
| ภก.สมชาย เภสัชกร | PHA001 | 1234     | Admin |
| ภญ.สมหญิง ยาดี   | PHA002 | 1234     | User  |
| ภก.วิชัย ตาสว่าง | PHA003 | 1234     | User  |

---

### ขั้นตอนที่ 7: Build สำหรับ Production

```bash
npm run build
```

ไฟล์ที่ build แล้วจะอยู่ในโฟลเดอร์ `dist/`

#### Preview Production Build:

```bash
npm run preview
```

---

## 📁 โครงสร้างโปรเจกต์

```
utth-ed/
├── public/                     # Static assets (icons, images)
├── src/
│   ├── components/
│   │   ├── layout/
│   │   │   └── Layout.tsx      # Sidebar + Header layout
│   │   ├── ui/                 # Reusable UI components
│   │   ├── EditPrepModal.tsx   # Modal แก้ไขรายการเตรียมยา
│   │   ├── PrepDetailsModal.tsx # Modal แสดงรายละเอียด
│   │   ├── ProfileCard.tsx     # Card โปรไฟล์ผู้ใช้
│   │   └── SummaryDetailsModal.tsx
│   ├── contexts/
│   │   ├── AuthContext.tsx     # จัดการ Authentication state
│   │   └── ToastContext.tsx    # จัดการ Toast notifications
│   ├── hooks/
│   │   ├── useFormulas.ts      # Hook: CRUD สูตรยา
│   │   ├── usePreps.ts         # Hook: CRUD การเตรียมยา
│   │   └── useUsers.ts        # Hook: CRUD ผู้ใช้
│   ├── lib/
│   │   ├── print.ts           # ฟังก์ชันพิมพ์ฉลาก/Batch Sheet
│   │   ├── supabase.ts        # Supabase client instance
│   │   └── utils.ts           # Utility functions
│   ├── pages/
│   │   ├── LoginPage.tsx      # หน้า Login
│   │   ├── StationSelectionPage.tsx  # เลือกห้องทำงาน
│   │   ├── DashboardPage.tsx  # หน้าหลัก (Dashboard)
│   │   ├── PreparePage.tsx    # บันทึกการเตรียมยา
│   │   ├── HistoryPage.tsx    # ประวัติการเตรียมยา
│   │   ├── FormulasPage.tsx   # จัดการสูตรยา
│   │   ├── UsersPage.tsx      # จัดการผู้ใช้ (Admin)
│   │   ├── ProfilePage.tsx    # โปรไฟล์
│   │   └── ForceChangePasswordPage.tsx  # เปลี่ยนรหัสผ่านครั้งแรก
│   ├── types/                 # TypeScript type definitions
│   ├── App.tsx                # Main app + routing
│   ├── main.tsx               # Entry point
│   └── index.css              # Global styles
├── .env                       # Environment variables (อย่า commit!)
├── index.html                 # HTML entry point
├── package.json               # Dependencies & scripts
├── vite.config.ts             # Vite configuration + PWA
├── supabase_schema.sql        # Database schema
├── supabase_migration_*.sql   # Database migrations
├── tsconfig.json              # TypeScript config
└── eslint.config.js           # ESLint config
```

---

## 🗄 ฐานข้อมูล

### ตาราง (Tables)

#### 1. `users` — ผู้ใช้งาน

| คอลัมน์                | ชนิดข้อมูล    | รายละเอียด                     |
| ---------------------- | ------------- | ------------------------------ |
| `id`                   | SERIAL (PK)   | รหัสผู้ใช้                     |
| `name`                 | TEXT          | ชื่อ-นามสกุล                   |
| `pha_id`               | TEXT (UNIQUE) | รหัสเภสัชกร (e.g., PHA001)     |
| `password`             | TEXT          | รหัสผ่าน                       |
| `role`                 | TEXT          | สิทธิ์ (`admin` / `user`)      |
| `active`               | BOOLEAN       | สถานะการใช้งาน                 |
| `profile_image`        | TEXT          | รูปโปรไฟล์ (`male` / `female`) |
| `must_change_password` | BOOLEAN       | บังคับเปลี่ยนรหัสผ่าน          |
| `created_at`           | TIMESTAMPTZ   | วันที่สร้าง                    |

#### 2. `formulas` — สูตรยา

| คอลัมน์         | ชนิดข้อมูล  | รายละเอียด                                   |
| --------------- | ----------- | -------------------------------------------- |
| `id`            | SERIAL (PK) | รหัสสูตรยา                                   |
| `name`          | TEXT        | ชื่อสูตรยา                                   |
| `description`   | TEXT        | รายละเอียด                                   |
| `concentration` | TEXT        | ความเข้มข้น                                  |
| `expiry_days`   | INTEGER     | จำนวนวันหมดอายุ (default: 7)                 |
| `category`      | TEXT        | หมวดหมู่ (antibiotic, antifungal, lubricant) |
| `price`         | INTEGER     | ราคา                                         |
| `ingredients`   | TEXT        | ส่วนประกอบ                                   |
| `method`        | TEXT        | วิธีเตรียม                                   |
| `created_at`    | TIMESTAMPTZ | วันที่สร้าง                                  |

#### 3. `preps` — บันทึกการเตรียมยา

| คอลัมน์         | ชนิดข้อมูล   | รายละเอียด                      |
| --------------- | ------------ | ------------------------------- |
| `id`            | SERIAL (PK)  | รหัสบันทึก                      |
| `formula_id`    | INTEGER (FK) | อ้างอิงสูตรยา                   |
| `formula_name`  | TEXT         | ชื่อสูตรยา                      |
| `concentration` | TEXT         | ความเข้มข้น                     |
| `mode`          | TEXT         | ประเภท (`patient` / `stock`)    |
| `target`        | TEXT         | เป้าหมาย                        |
| `hn`            | TEXT         | Hospital Number (เฉพาะ patient) |
| `patient_name`  | TEXT         | ชื่อผู้ป่วย                     |
| `dest_room`     | TEXT         | ห้องปลายทาง                     |
| `lot_no`        | TEXT         | เลข Lot                         |
| `date`          | DATE         | วันที่เตรียม                    |
| `expiry_date`   | DATE         | วันหมดอายุ                      |
| `qty`           | INTEGER      | จำนวน                           |
| `note`          | TEXT         | หมายเหตุ                        |
| `prepared_by`   | TEXT         | ผู้เตรียม                       |
| `location`      | TEXT         | สถานที่/ห้อง                    |
| `created_at`    | TIMESTAMPTZ  | วันที่สร้างบันทึก               |

### ER Diagram

```
┌──────────┐       ┌──────────────┐       ┌──────────┐
│  users   │       │    preps     │       │ formulas │
├──────────┤       ├──────────────┤       ├──────────┤
│ id (PK)  │       │ id (PK)      │       │ id (PK)  │
│ name     │       │ formula_id ──┼──FK──▶│ name     │
│ pha_id   │       │ prepared_by  │       │ conc.    │
│ password │       │ patient_name │       │ category │
│ role     │       │ lot_no       │       │ method   │
│ active   │       │ date         │       │ ...      │
│ ...      │       │ ...          │       └──────────┘
└──────────┘       └──────────────┘
```

---

## 🚀 การ Deploy

### Deploy ไปยัง GitHub Pages

```bash
# Build และ Deploy ในคำสั่งเดียว
npm run deploy
```

คำสั่งนี้จะ:

1. รัน `npm run build` (build production bundle)
2. Push โฟลเดอร์ `dist/` ไปยัง branch `gh-pages`

**URL หลังจาก Deploy**:

```
https://codex074.github.io/ocuprep/
```

---

## 📝 คำสั่งที่ใช้บ่อย

| คำสั่ง            | รายละเอียด                        |
| ----------------- | --------------------------------- |
| `npm run dev`     | เปิด Dev Server (Hot Reload)      |
| `npm run build`   | Build สำหรับ Production           |
| `npm run preview` | Preview Production Build ใน local |
| `npm run lint`    | ตรวจสอบ code ด้วย ESLint          |
| `npm run deploy`  | Build + Deploy ไปยัง GitHub Pages |

---

> 📅 **อัปเดตล่าสุด**: กุมภาพันธ์ 2569 (2026)
> 📝 **ผู้จัดทำ**: ระบบ Auto-generated Documentation
